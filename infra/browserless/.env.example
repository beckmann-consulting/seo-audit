# Copy to .env in this directory. Generate a strong random token:
#   openssl rand -hex 32
# The same value must appear in seo-audit/.env.local as BROWSERLESS_TOKEN
# so the audit service authenticates against the container.
BROWSERLESS_TOKEN=replace-with-openssl-rand-hex-32
